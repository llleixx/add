name: actions
on:
 push:
  branches:
   - dev
jobs:
 add-check:
  runs-on: ubuntu-latest
  steps:
   - uses: actions/checkout@v3
   - name: Run a shell
     run: ./t.sh
   - name: push
     uses: JamesIves/github-pages-deploy-action@dev
     with:
      branch: master
      force: true
